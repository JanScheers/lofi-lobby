{
  "name": "lofi-lobby",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "update-game": "node scripts/update-game.mjs",
    "remove-game": "node scripts/remove-game.mjs",
    "install:renpy": "node scripts/install-renpy.mjs",
    "postinstall": "node -e \"if (!process.env.INSTALL_RENPY) process.exit(0); require('child_process').execSync('node scripts/install-renpy.mjs', {stdio:'inherit'})\"",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "astro": "^5.17.1"
  }
}
